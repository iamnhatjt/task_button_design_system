enum SLShape {
  rounded,
  pill,
}

enum SLState {
  defaultState,
  hover,
  active,
}

enum SLSize {
  large,
  medium,
  small,
}

enum SLType {
  soild,
  outline,
  ghost,
}

enum SLContentType {
  textOnly,
  leadingIcon,
  trailingIcon,
  iconOnly,
}
